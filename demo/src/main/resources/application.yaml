spring:
  application:
    name: demo

  datasource:
      url: jdbc:h2:mem:demo
      driver-class-name: org.h2.Driver
      username: admin
      password: admin
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sq : true
server:
  port: 8787